#  Copyright (C) 2003  CEA/DEN, EDF R&D
#
#
#
#  File   : Makefile.in
#  Author : Vasily Rusyaev (Open Cascade NN)
#  Module : doc
#  $Header: 

top_srcdir=@top_srcdir@
top_builddir=../../..
srcdir=@srcdir@
VPATH=.:@srcdir@

@COMMENCE@

usr_docs:
	cp -fr $(srcdir)/GUI ./
	-find $(PWD) -name CVS -prune -exec rm -rf {} \;
	ln -sf ${KERNEL_ROOT_DIR}/share/doc/salome/index.html ./GUI/kernel_index.html
	ln -sf ${KERNEL_ROOT_DIR}/share/doc/salome/INSTALL.html ./GUI/INSTALL.html
	ln -sf ${KERNEL_ROOT_DIR}/share/doc/salome/UnitTests.html ./GUI/UnitTests.html
	ln -sf ${KERNEL_ROOT_DIR}/share/doc/salome/SALOME_Application.html ./GUI/SALOME_Application.html

docs: usr_docs

clean:
	rm -fr `ls | grep -v "Makefile"`

distclean: clean
	rm -fr GUI

install:
	mkdir -p $(docdir)/gui
	cp -rf GUI $(docdir)/gui
	-find $(PWD) -name CVS -prune -exec rm -rf {} \;

uninstall:
	rm -rf $(docdir)/gui/GUI
